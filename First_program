#include <vector>
#include <iostream>
using namespace std;
 
vector<int> parent(10, -1);
 
int main()
{
    int x = 0;
    while (cin >> x) {
        parent.push_back(x);
    }
    for (int i = 0; i < parent.size(); ++i) {
        cout << parent[i] << " ";
    }
    return 0;
}
